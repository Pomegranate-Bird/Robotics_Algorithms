import numpy as np

my_arra = np.arange(8) # Creates an array starting with 0 to 8, excluding the last index
my_arr = np.arange(1, 8) # Creates an array starting with 1 to 8, excluding the last index
my_array = np.arange(1, 8, 2) #  # Creates an array starting with that starts with 1 and then adds 2 giving only odds (step increased by 2)

print(my_array) # Prints the array 
print(type(my_array)) # type numpy array